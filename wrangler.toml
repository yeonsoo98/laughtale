# Cloudflare 배포 설정 — 정적 사이트(정적 에셋만) 방식.
# 이 파일이 있으면 `wrangler deploy` 가 SSR 어댑터 자동 설치를 건너뛰고
# dist 폴더를 그대로 정적 파일로 업로드한다.
name = "laughtale"
compatibility_date = "2025-06-01"

[assets]
directory = "./dist"
# 없는 경로 요청 시 우리가 만든 404.html 을 보여준다.
not_found_handling = "404-page"
